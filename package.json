{
  "name": "spooncast-server-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "dependencies": {
    "serverless-kms-secrets": "^1.0.3",
    "serverless-plugin-lambda-warmup": "^1.0.1",
    "serverless-pseudo-parameters": "^2.2.0",
    "serverless-python-requirements": "^4.2.4"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
